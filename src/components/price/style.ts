import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  gray: {
    fontSize: 14,
    fontWeight: "500",
    lineHeight: 16,
    color: "#888888",
  },
  darkGray: {
    fontSize: 12,
    fontWeight: "500",
    lineHeight: 14,
    color: "#7C7C8A",
  },
});
