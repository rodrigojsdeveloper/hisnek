type Product {
  id: ID!
  name: String!
  price: Float!
  img: String!
  description: String!
}

type Query {
  getProducts: [Product!]!
  getCartProducts: [Product!]!
  subTotal: Int!
  quantity: Int!
  findProduct: [Product!]!
}
